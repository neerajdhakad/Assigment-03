namespace LinkedList
{
    internal class Node
    {
        protected internal int data;
        protected internal Node next;

        internal Node(int value)
        {
            data = value;
            next = null;
        }
    }
}
